# User Taste Preferences
- Uses TypeScript with strict mode across all packages. Confidence: 0.95
- Uses npm workspaces for monorepo management. Confidence: 0.95
- Uses Node.js as runtime for daemon and relay server (uses Node APIs like `node:crypto`, `node:fs`, `node:path`). Confidence: 0.95
- Uses vitest for unit and integration testing. Confidence: 0.9
- Prefers monorepo with a shared package (`@remotecmd/shared`) for protocol, crypto, and utilities consumed by all other packages. Confidence: 0.95
- Prefers cloud relay connectivity over LAN — both phone and desktop dial OUT to a relay server, no port forwarding needed. Confidence: 0.9
- Prefers PWA for mobile UI over native app — one codebase, installable via "Add to Home Screen". Confidence: 0.9
- Prefers self-hostable relay server over a managed service. Confidence: 0.9
- Prefers `node-pty` for cross-platform PTY/shell support (ConPTY on Windows 1809+). Confidence: 0.9
- Prefers keeping sessions alive on the daemon side (PTYs outlive phone disconnects) with scrollback ring buffer for reconnect replay. Confidence: 0.9
- Prefers comprehensive test coverage including real subprocess tests (actual PTY spawn, shell echo). Confidence: 0.95
- Uses Zod for protocol validation at message boundaries. Confidence: 0.95
- Prefers end-to-end integration tests with real WebSocket connections over mocked stacks. Confidence: 0.9
- Prefers adding targeted debug instrumentation (console.log, HTTP debug endpoints) when troubleshooting rather than building extensive logging infrastructure upfront. Confidence: 0.85
- Uses `@xterm/xterm` + `@xterm/addon-fit` for terminal rendering in the PWA. Confidence: 0.95
- Uses `tweetnacl` for E2E crypto (X25519 key agreement, NaCl secretbox). Confidence: 0.9
- Uses `better-sqlite3` for relay's device/token registry (WAL mode). Confidence: 0.9
- Uses `fastify` for relay HTTP server (with `@fastify/static` for serving PWA). Confidence: 0.9
- Uses `commander` for daemon and relay CLI. Confidence: 0.9
- Uses `vite` + `vite-plugin-pwa` for the web PWA build. Confidence: 0.9
- Develops on Windows (paths like `D:\ExperimentalPocs\remotecmd`). Avoids Unix-specific shell idioms (`tail`, `;` chains). Confidence: 0.95
- Shell is Windows PowerShell 5.1, not PS7 — PS7-only parameters like `Invoke-WebRequest -SkipHttpErrorCheck` fail. For HTTP status checks use `curl.exe` explicitly (bare `curl` resolves to the Invoke-WebRequest alias), e.g. `curl.exe -s -o NUL -w "%{http_code}" <url>`. Confidence: 0.85
- The `powershell` tool on this machine swallows output behind CLIXML serialization noise (commands appear to produce nothing) — for quick filesystem/port/process checks use cmd-syntax `shell_command` instead (`dir`, `type`, `netstat -ano | findstr`, `echo %TIME% %DATE%`). Confidence: 0.7
- A release isn't "done" until every published artifact is verified live: `npm view <pkg> version` for each package, an HTTP status check on the Docker Hub tag, and `gh run list`/`gh run watch` on the tag-triggered workflow, with results reported in the final summary. Confidence: 0.75
- NODE_ENV is set to `production` on this machine, causing npm to skip devDependencies by default. Must use `npm install --include=dev` explicitly. Confidence: 0.95
- Commander resolution can be shadowed by transitive nested `node_modules` (e.g. vite bundling its own copy). Each package must directly declare its CLI dependencies. Confidence: 0.9
- Background task output (`run_in_background: true`) may not be captured in log files — prefers foreground execution for seeing immediate output during debugging. Confidence: 0.85
- Prefers writing dedicated Node.js scripts to manage and verify complex multi-process setups (e.g. `scripts/run-e2e.mjs`). Confidence: 0.9
- Prefers HTTP debug endpoints over raw DB inspection scripts. Confidence: 0.85
- Fixes issues incrementally: identify root cause, apply minimal fix, rebuild and re-run. Avoids large refactors for isolated bugs. Confidence: 0.9
- Writes a README with explicit run commands for each setup mode (Docker, self-hosted Node, development). Confidence: 0.95
- Provides Dockerfile with multi-stage build for the relay. Confidence: 0.9
- Sets up GitHub Actions CI with matrix strategy covering multiple Node versions and OS platforms. Confidence: 0.9
- Uses Zod discriminated unions (`z.discriminatedUnion`) for protocol message types. Confidence: 0.9
- Uses `noUncheckedIndexedAccess` in tsconfig for array access safety. Confidence: 0.85
- Prefers explicit cleanup functions returned from `useEffect` in React over effect-aggregation patterns. Confidence: 0.85
- Prefers `storeToken` / `storeDaemonToken` as separate methods to avoid `INSERT OR REPLACE` with UNIQUE constraint conflicts (same token hash for different roles). Confidence: 0.9
- When handing off a running app, wants the agent to actually start the servers/services AND provide exact copy-paste terminal commands to run them independently later. Confidence: 0.75
- Prefers hands-on verification of user-facing flows before claiming success — e.g. driving a browser emulator as the "phone" (agent-browser) to walk through pairing/QR flows and screenshot the results. Confidence: 0.75
- For open-source projects, demands radically simple onboarding: a single command to start (`npx <tool>`), automatic detection (e.g. LAN IPs for QR URLs), QR-code pairing printed in the terminal, and idempotent re-runs. Explicitly rejected multi-step manual setup as "so confusing, it's hard for the user to start". Confidence: 0.9
- Ships server software through two canonical distribution channels: an npx one-liner and a runnable Docker container (`docker run` + compose file), bundling any web UI into the npm packages so users need zero extra configuration. Confidence: 0.85
- Builds tools he intends to open-source with a companion hosted service he operates himself — tools should support both a local/LAN mode and a `connect`-style hosted mode pointing at his public server (via a `DEFAULT_HOSTED_RELAY`-style constant that's easy to change before publishing). Confidence: 0.8
- Publishes open-source tools to the public npm registry so `npx <tool>` works for end users — GitHub alone is not a distribution channel to him. Release workflow: workspace-scoped `npm publish -w packages/<pkg>`, `--access public` for scoped packages, concrete `^x.y.z` versions replacing workspace `*` deps, and dry-run verification before the real publish. Confidence: 0.8
- GitHub account/repo: `rvenky125/rcmdsh` — used for repository/bugs/homepage metadata and GHCR image paths. Confidence: 0.9
- Docker Hub account: `venkypaithireddy9390` — publishes Docker images there (e.g. `venkypaithireddy9390/rcmdsh` with a `v*` version tag plus `latest`); Docker Hub, not GHCR, is the real registry, and `ghcr.io/YOURNAME/...` references in README/docker-compose were stale placeholders. Confidence: 0.85
- Delegates release/publishing mechanics to the agent (metadata prep, npm name availability checks, tarball dry-runs) and handles only the interactive authentication steps himself (`npm login` browser/2FA flow). Confidence: 0.7
- Willingly pastes secrets (npm automation tokens, etc.) directly into chat for the agent to use, without flagging them as sensitive. Confidence: 0.8
- Prefers publishing npm packages unscoped over scoped packages for personal/individual open-source projects — avoids the friction of having to claim a new scope on the npm account via the website before publishing. Would rather use descriptive unscoped names like `rcmdsh-core`, `rcmdsh-relay` than `@org/pkg` for sub-packages. Confidence: 0.85
- When publishing package updates after a breaking internal change (e.g. import path renames, dependency restructuring), expects a patch/minor version bump and a clean re-publish rather than patching the broken published version. Comfortable with multiple `0.1.x` versions on the registry, with the option to deprecate the broken older one after the new one is live. Confidence: 0.75
- When releasing artifacts (docker push, npm publish), wants docs updated in the same pass — "update the docs as well" — so README run commands and docker-compose reference the actually-published image/registry instead of placeholder paths. Confidence: 0.75
- Prefers the fuller-featured option over the minimal one when choosing between implementation approaches — picked the native terminal TUI over the "Recommended" reuse-the-browser-PWA option for PC-side control, prioritizing a complete native UX even at higher effort. Confidence: 0.75
- Workflow style: gives terse go-aheads ("continue"), answers structured multiple-choice scope questions decisively, approves the written plan, then expects the agent to implement autonomously with a phased todo list. Confidence: 0.7
- Wants processes/sessions created remotely to also be visible and controllable on the local machine (a local TUI/attach path, PID shown in the session list) — headless invisible background processes were his core complaint ("there is no control over that"). Later refined: this visibility must come from a browser dashboard, NOT from terminal windows popping up on screen — the visible terminal window was explicitly rejected ("I can still see the terminal window is showing up which I don't want"). Confidence: 0.8
- Prefers reliable, well-engineered mechanisms over experimental hacks when the tradeoff is explained: declined an experimental screen-scrape + keystroke-injection "takeover" mode (degraded — no resize, repaint-based rendering, flaky under Windows Terminal) in favor of the clean attach-based design, even though takeover would have covered more cases. Confidence: 0.7
- Stress-tests proposed designs for real-world edge cases and wants platform limitations surfaced honestly before committing — asked mid-implementation whether attach could reach a CLI already running in the window, then accepted the documented Windows ConPTY limitation once explained. Confidence: 0.6
- Excludes tooling state files (`.commandcode`) from git staging — commits contain only project sources, docs, lockfile, and scripts. Confidence: 0.6
- Treats the agent as the release engineer: trusts it to discover scope (which packages need bumping, whether a feature justifies a minor vs patch, what order to publish workspaces in), while only contributing the high-level directive ("publish into npmjs as well"). Expects the agent to investigate registry state, dry-run, and execute without further approval unless a real ambiguity arises. Confidence: 0.8
- Pre-release semver discipline for `0.x` projects: bumps internal workspace dep ranges (e.g. `^0.2.0` → `^0.3.0`) in lockfile-aware `npm install` before publishing, because `^0.x` doesn't cross minors. Doesn't leave stale `^` ranges pointing at older published versions when shipping a new minor under 1.0. Confidence: 0.9
- Fixes stale hardcoded constants surfaced during release prep (e.g. `VERSION = "0.2.0"` in source when package.json was 0.3.0) in the same commit/publish, rather than letting the divergence ship again. Treats version drift between source and package.json as a real bug, not a cosmetic issue. Confidence: 0.85
- Host startup/connection UX (revised): the default startup view is plain console output that keeps the pairable QR code visible plus a "connection started" message — no full-screen interactive TUI by default (a TUI clears the screen and hides the QR until you press q), and no auto-opened browser window; the browser-dashboard idea was explicitly retracted ("no need with browser window, sorry I've mistaken"). The interactive session screen is opt-in via a flag (`rcmdsh --tui`). Confidence: 0.85
- QR codes and their URLs must carry everything needed to auto-complete the flow: deep-link the pairing code into the URL (e.g. `.../#/pair?relay=...&code=...`) so scanning prefills and auto-submits with zero manual typing or extra taps — a QR pointing at the bare app URL was flagged as a bug ("if I scan the qr the code isn't autofilling"). Confidence: 0.85
- A one-word "deploy" directive runs the entire release pipeline end-to-end with no further check-ins: bump versions (all package.json files, the `VERSION` constant, workspace dep ranges, lockfile refresh) → gate on typecheck + tests + build → commit and push to master → publish npm packages in dependency order (rcmdsh-core → rcmdsh-relay → rcmdsh, rebuilding each workspace first, `--access=public`) → push master + `vX.Y.Z` tag (triggers the Docker CI → GHCR) → copy the GHCR image to Docker Hub → verify every artifact live. The release commit message is version-prefixed with a bullet changelog, written to a scratchpad file and applied via `git commit -F`. Confidence: 0.85
- Publishes multi-arch Docker images (linux/amd64 + linux/arm64) through buildx using a temporary docker-container builder — created, used with `--push` tagging both `vX.Y.Z` and `latest`, then removed after the release. Confidence: 0.75
- After npm publish, downloads the published tarball from registry.npmjs.org and inspects its contents to confirm no stale files shipped (a stale-tarball incident in an earlier release made this a standard post-publish check). Confidence: 0.7
- Expects cross-device consistency in what he builds: the same session/screen opened on both phone and desktop (QR re-scan flow) must adapt its rendering/geometry to whichever device is viewing it — a layout or size locked to the creating device is a bug he actively reports ("can't we make it resizeable to wherever we opens it?"). Confidence: 0.7
- Monorepo typecheck mechanics: packages typecheck against the built dist of workspace deps, so rebuild `rcmdsh-core` (`npm run build -w rcmdsh-core`) before running the root `npm run typecheck`; the root typecheck script doesn't cover the web package — check it separately with `npx tsc --noEmit -p packages/web`. Confidence: 0.8
- `AttachConsole failed` noise after `npm test` on this machine is a harmless node-pty/ConPTY teardown artifact on Windows — treat the run as green when the test results pass. Confidence: 0.75
- Demands clear, state-aware error messages over generic ones — a vague failure hint like "could not reach the daemon — is it running?" was flagged as "so much confusing"; CLI errors should diagnose the actual condition (daemon not running vs. stuck waiting for pairing vs. port taken by something else) and tell him exactly what to do next. Confidence: 0.85
- A fix isn't delivered until his local run actually picks it up: `npx rcmdsh` resolves to the local workspace dist (`node_modules/.bin` link → `packages/daemon/dist`), so `npm run build` must be run after source edits before asking him to re-test; for web/PWA changes, the service worker precaches the old bundle — he must hard-refresh (twice) to see the new UI. Also: a daemon process he started earlier can be running from the npm-cache copy of the *published* package (not the local build) — when he reports "still broken", first check which binary the running process came from (via its command line) and have him restart it from the repo before diagnosing further. Confidence: 0.8
- Docker image distribution happens through the tag-triggered GitHub Actions workflow (`docker.yml` on `v*` tags → multi-arch buildx → GHCR + Docker Hub), so releases don't need a local Docker daemon (it wasn't even running this deploy). Known traps: the workflow masks push failures with `continue-on-error` (run showed green while Docker Hub `latest` stayed stale — caught only by checking the Docker Hub tags API), and the `DOCKERHUB_TOKEN` GitHub secret expires (401 insufficient scopes) — fix by rotating the secret (`gh secret set DOCKERHUB_TOKEN`) and re-running the workflow, never by pushing a single-arch image from local Docker over the multi-arch manifest (breaks ARM pulls). Confidence: 0.75
- Operates a hosted/public relay at `rcmdsh.vendroid.dev` alongside the self-hosted story — it runs on his own Docker host (not reachable from the dev machine), is fronted by Cloudflare, and is in active production use (he tests it by hand when debugging, e.g. the ads.txt rollout). Remote debugging there means handing him exact server-side commands to run and paste back: `docker exec <c> ls -la /app/public`, `docker inspect <c> --format '{{json .Mounts}}'` (a bind mount over the served directory can explain missing files), and `curl localhost:8787/...` on origin vs the public URL (origin-vs-CDN-cache divergence). Confidence: 0.7
- When a release step is blocked (e.g. expired `DOCKERHUB_TOKEN` in CI), he still expects the deploy to be finished in-session rather than deferred to a user action — "push docker image as well, we did it earlier". The accepted fallback: copy the already-built public multi-arch GHCR image straight to Docker Hub (`docker buildx imagetools create -t <hub>:<ver> -t <hub>:<minor-line> -t <hub>:latest ghcr.io/<img>:<ver>`) — byte-identical multi-arch artifact, no rebuild, ARM preserved (never a single-arch local push). Caveat: the create re-uploads every layer (~300MB, no cross-registry mounts), so a foreground call can time out mid-blobs; run it in the background and verify with `imagetools inspect` + the Docker Hub tags API — a retry after a timed-out attempt finalizes quickly since blobs persist. Confidence: 0.8
- Treats secret exposure through agent-managed files as a real risk to verify, not assume: proactively asked "we're not exposing any secret keys using this taste file right?" before letting tracked agent-state (taste file, settings.json, DB sidecars) ride along in the repo. Taste/state files must contain only non-sensitive metadata (usernames, registry accounts, domains, workflow habits) — never tokens or keys — and "no secrets exposed" claims should be backed by an actual audit (grep tracked files for real token patterns, inspect committed blobs) rather than asserted. (Distinct context from chat, where he pastes secrets freely — his concern is what lands in tracked/public files.) Confidence: 0.75
- Monetizes the web PWA with Google AdSense: a closable interstitial ad overlay (`AdOverlay` component + async AdSense loader in `index.html`, publisher `ca-pub-2467417149555465`, slot `1365730124`, responsive unit) shows on every page load/refresh and re-shows 10 minutes after being dismissed; the app must always remain reachable after closing (if the ad script is blocked the box stays empty but the ✕ still works). He followed up to complete the setup with an `ads.txt` for the same publisher (`pub-2467417149555465`) served from the PWA so the hosted relay answers `/ads.txt`. Confidence: 0.85
- Static PWA assets that must live at a fixed URL (e.g. `ads.txt`) go in `packages/web/public/` — after adding one, rebuild the web package (`npm run build -w @rcmdsh/web`) and run `node scripts/copy-web-dist.mjs`, then confirm the file reached all three bundles (`packages/web/dist`, `packages/relay/public`, `packages/daemon/public`); it ships in the npm tarballs because `public` is in their `files` list, and the relay serves real static files directly (the SPA fallback only covers unknown paths). Confidence: 0.7
- When his direct observation contradicts the agent's running diagnosis, take the observation as likely correct and re-verify with hard evidence instead of restating the theory — told the hosted relay was still on the old image, he pushed back ("how that can be possible, I can see the ad pop, that's from new image only right?") and was right: the live bundle hash proved the new build was deployed and the real fault was narrower (`ads.txt` missing from the served directory). Confidence: 0.7
- `.dockerignore` over-broad globs are a silent release-killer in this repo: `packages/*/public` excluded the *source* `packages/web/public` from the Docker build context, so every CI-built image shipped without the files vite copies from it (`ads.txt`, `icon.svg` — the favicon had been 404→SPA-fallback in Docker deploys all along) while npm tarballs were unaffected. The ignore list now excludes only *generated* folders (`packages/daemon/public`, `packages/relay/public`) — keep source asset folders in the Docker build context. Confidence: 0.8
- After a Docker image rebuild meant to fix missing/incorrect content, verify the artifact itself before telling him to pull: pull the layers straight from the registry (GHCR anonymous token → manifest → blob download → `tar -tzf` each layer) and confirm the expected files are inside, rather than trusting a green CI run. Registry inspection isn't the finish line — he followed up with "verify it in the local docker as well": pull the actually-pushed tag from Docker Hub, confirm the digest matches the verified push, `docker run --rm --entrypoint ls <img> -la /app/public` to confirm the fixed files inside the image, then run the container and curl the fixed endpoints (status + content-type + body) for a live 200 check before declaring it shipped. Confidence: 0.75
- Local Docker verification mechanics on the dev machine: Docker Desktop isn't running by default — launch it (`start "" "C:\Program Files\Docker\Docker\Docker Desktop.exe"`) and wait ~25s for the engine before `docker` commands. His own local relay already squats host port 8787, so run throwaway verification containers on a spare high port (e.g. 17870) instead of clobbering his running local services, and `docker rm -f` the test container when done. Confidence: 0.6
- When a bug appears only in the browser and not in curl ("it's working fine with curl... but if I open the same file in web it's again navigating to root, try it if you've web access"), reproduce it with real browser automation rather than theorizing from HTTP checks: agent-browser + screenshots, first from a clean browser to confirm the server side, then in the exact triggering state (service worker installed and controlling the page) to isolate the cause. curl has no service worker, so green curl checks don't rule out browser-only bugs — he explicitly expects the agent to use its own web access to verify. Close the browser session and clean up scratch dirs afterward. Confidence: 0.75
- PWA service-worker trap in this repo: `navigateFallback: "/index.html"` answers *every* navigation with the app shell — including real files like `/ads.txt` — for any browser with the SW installed (invisible to curl). Fixed in v0.3.6 with `navigateFallbackDenylist: [/\/[^/]+\.[a-zA-Z0-9]+$/]` in the vite-plugin-pwa workbox config — the app is hash-routed so every legit navigation targets `/`, and anything with a file extension is a real asset that must bypass the fallback. After changing SW config, verify the denylist landed in the generated `packages/web/dist/sw.js` and that hash-route deep links (`/#/pair?relay=...&code=...`) still load the app before releasing. Confidence: 0.75
