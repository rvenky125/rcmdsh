services:
  relay:
    build: .
    # or use a published image:
    # image: venkypaithireddy9390/rcmdsh:latest
    ports:
      - "8787:8787"
    volumes:
      - relay-data:/data
    restart: unless-stopped

volumes:
  relay-data:
